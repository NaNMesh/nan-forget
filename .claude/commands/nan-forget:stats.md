# nan-forget:stats — Memory Health

Show memory statistics: active/archived counts, breakdown by type and project.

## Instructions

Try `memory_stats` MCP tool first. If unavailable, run `npx nan-forget stats` via Bash.

Display the results in a clean, readable format.

{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.NaNMesh/nan-forget",
  "description": "AI that remembers. Local-first cross-session, cross-project long-term memory for coding tools.",
  "repository": {
    "url": "https://github.com/NaNMesh/nan-forget",
    "source": "github"
  },
  "version": "0.6.6",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "nan-forget",
      "version": "0.6.6",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "OPENAI_API_KEY",
          "description": "OpenAI API key for embeddings. If not set, falls back to local Ollama.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "NAN_FORGET_EMBEDDING_PROVIDER",
          "description": "Embedding provider: openai or ollama",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "default": "ollama"
        },
        {
          "name": "NAN_FORGET_USER_ID",
          "description": "User identifier for multi-user setups",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "default": "default"
        },
        {
          "name": "NAN_FORGET_PROJECT_ROOT",
          "description": "Project root used for local memory file sync and cleanup",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        }
      ]
    }
  ]
}
